{
  "name": "@repo/example-with-supabase-client",
  "private": true,
  "type": "module",
  "scripts": {
    "codegen": "apigen gen --migrations src/db/migrations --out src/api.gen.ts --module @repo/apigen",
    "start:api": "bun run src/index.ts",
    "start:client": "bun run src/client.ts",
    "build": "bun build src/index.ts --outdir dist --target bun",
    "check:types": "tsc"
  },
  "dependencies": {
    "@repo/apigen": "workspace:*",
    "@supabase/supabase-js": "^2.110.7"
  },
  "devDependencies": {
    "@repo/typescript-config": "workspace:*",
    "@types/bun": "catalog:",
    "typescript": "catalog:",
    "@electric-sql/pglite": "catalog:"
  }
}
